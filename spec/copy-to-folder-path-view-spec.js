'use babel';

import CopyToFolderPathView from '../lib/copy-to-folder-path-view';

describe('CopyToFolderPathView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
